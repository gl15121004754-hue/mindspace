# MindSpace

面向高敏感人群（HSP）及高压都市女性的 AI 情绪急救与长期陪伴应用。本 CONTEXT 是产品的语言共识表，只记录领域概念，不记录实现细节。

## Language

### 情绪记录

**情绪记录（Emotion Record）**:
用户对一个情绪事件的结构化记录，是 MindSpace 所有洞察与 AI 反馈的数据原子。每条记录包含情绪类型、强度，可选文字描述和触发因素。
_Avoid_: 日记、打卡、心情签到、check-in

**记录来源（Record Source）**:
一条情绪记录的产生场景，决定它的重量和用途。同一个 `EmotionRecord` 类型下有三种来源：
- **每日记录**：日常非危机时刻的轻量记录，必选情绪+强度，文字可选。
- **SOS 记录**：急性情绪危机时、走 60s 急救流程产生的记录，重量更高。
- **对话记录**：从 AI 深度对话中自动提取情绪标签生成的记录（占位，待定）。
_Avoid_: 把来源当作独立的数据模型。来源是同一条情绪记录的属性，不是另一个实体。

### AI 反馈

**AI 反馈（AI Reflection）**:
用户提交每日记录后，AI 基于该笔记录内容（若有文字）结合用户历史情绪数据生成的、贴合用户当下状态的一段回应。它是"每日记录"闭环中"明日再来的拉力"来源。形态为**单次回应**（不可在该页继续多轮），回应后通过结构化选项流转到对话（chat）或急救（SOS），与 `/chat` 的多轮深聊形成边界。
_Avoid_: 每日推送、每日内容、每日鸡汤、daily content。这些词暗示内容由平台单向生产，与"基于用户记录即时生成"的本意冲突。也避免称其为"对话"，因为它不是多轮的。

**AI 反馈存档（Reflection Archive）**:
每条每日记录的 AI 反馈文字被持久化进对应的 `EmotionRecord`（`aiReflection` 字段），使用户回看历史记录时能看到"当时被陪伴的回应"，而非只剩干瘪的情绪标签。它属于情绪记录本身，不进入对话历史（`chatStore`）。

### 情绪集合

**情绪集合（Emotion Set）**:
每日记录可选的情绪类型，覆盖正面与负面以尊重情绪的真实全貌。负面沿用 SOS 的 6 种（焦虑/愤怒/悲伤/惊恐/过载/疲惫），正面新增 4 种（平静/开心/感激/放松），共 10 种。SOS 场景仍只使用负面子集。
_Avoid_: 把每日记录的情绪集合等同于 SOS 的负面子集。每日记录必须允许正面情绪，否则会把疗愈工具变成每日负面强化器。

**强度（Intensity）**:
1-10 的感受刻度，语义统一为"这种感受有多强烈"，不分正负。正面情绪的强度 8 表示"强烈地感受到开心"，负面情绪的强度 8 表示"强烈地感受到焦虑"。
_Avoid_: 把强度解释为"多难受"——这是 SOS 场景的旧语义，对正面情绪不成立。

### 记录回看

**今日卡片（Today Card）**:
首页展示的"今天已记"区块。当用户今日已有每日记录时，首页显示今天的情绪卡片 + AI 反馈摘要，让存档的陪伴痕迹在每天必经的高频页被看到。它不是独立页面，是首页的组成部分。

**记录时间线（Record Timeline）**:
独立的深度回看页面（路由 `/timeline`），按天聚合展示历史每日记录。折叠态显示每天的记录条目（情绪emoji+强度），点击某条展开查看当时的 AI 反馈原文。与洞察页（InsightPage，纯统计图表）职责互补：洞察页回答"我整体趋势怎样"，时间线回答"我某天记了什么、AI 怎么回应的"。
_Avoid_: 把时间线和洞察页混为一谈。两者一个是明细回看、一个是统计聚合。也不要把时间线做成首页的子区块——深度回看是低频行为，值得独立页面，而首页只放"今日"摘要。
